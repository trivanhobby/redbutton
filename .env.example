# Example environment variables for RedButton
# Copy this file to .env.local and add your values

# Your OpenAI API key for AI suggestions
# This will be used automatically if no key is set in the app settings
# You can get an API key from: https://platform.openai.com/account/api-keys
REACT_APP_OPENAI_API_KEY=

# Note: In production, you should use a secure backend service to handle API calls
# rather than exposing your API key in the browser. 